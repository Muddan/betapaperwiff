to use this database
just open directory where mongo is installed
now open cmd in that directory
 RUN mongod in cmd
open another terminal in same directory and run.

mongorestore --db paperwiff-new ~\betapaperwiff\database-backup\paperwiff-new\



to run flask on debug
set FLASK_ENV=development
python -m flask run